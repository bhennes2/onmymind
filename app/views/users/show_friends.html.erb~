<% if @users != [] %>
	<table>
		<tr>
			<th>Username</th>
			<th>Email</th>
			<th>Thoughts</th>
			<th>Action</th>
		</tr>
	<% @users.each do |user| %>
		<tr>
			<td><%= user.username %></td>
			<td><%= user.email %></td>
			<td><%= user.thoughts.count %></td>
			<td><%= link_to 'See my profile', user %></td>
		</tr>
	<% end %>
	</table>
<% else %>
	<p>No friends yet.  Better find some <%= link_to 'here', users_path %></p>
<% end %>
